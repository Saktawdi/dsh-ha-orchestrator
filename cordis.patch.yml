# dsh-ha-orchestrator bundle patch: inserted as a profile layer by `dsh plugin add`
# when this package declares dsh.bundle.patch. Last write wins per row id.
- insert:
    - id: dsh-ha-orchestrator
      name: dsh-ha-orchestrator
